# [Project Name] — AI Context

## Project Overview
- **Name**: [project-name]
- **Type**: [agentic product / traditional + agentic hybrid / TBD]
- **Stack**: [language · framework · LLM provider · vector store if any · verify exact versions via `npm view <pkg> version` / `pip index versions <pkg>` before committing — assistant knowledge cutoffs can lag stable releases by months]
- **Workflow**: adlc-flow plugin — phase-gated lifecycle per [`.claude/CONTEXT.md`](CONTEXT.md)

## What this project is for

[1-3 sentences describing the user-facing purpose. If agentic: name the specific behavior the LLM provides. If traditional: name the business domain.]

## Anti-patterns
❌ [Project-specific anti-pattern, e.g. "Logging PII to console"]
❌ [Project-specific anti-pattern]
❌ [Project-specific anti-pattern]

## Definition of Done
- [ ] Acceptance criteria verified by human at the relevant gate
- [ ] Phase-appropriate ADLC artifact updated (HYPOTHESIS.md · RESPONSIBILITY-MAP.md · GOLDEN-DATASET/ · EVAL-SUITE/ · OBSERVABILITY.md · etc.)
- [ ] CONTEXT.md / ARCHITECTURE.md updated if vocabulary or structure changed
- [ ] ADR written for hard-to-reverse decisions (in `docs/adr/`)
- [ ] Tests / evals run green (deterministic tests via `/tdd`; agent evals via `/eval-suite-planner` + `/pov-gate`)

## Behavioral Guidelines

### Think Before Acting
Surface assumptions before writing. Ask on ambiguous requirements.

### Eval Before Code (agentic features only)
Define eval strategy at AG (Architecture Gate) before any production agent code.

### Probabilistic Honesty (agentic features)
Single-pass test results are noise — report distributions over N runs with threshold-based pass.

### Goal-Driven Execution
Restate the task as a verifiable goal AND name its ADLC phase before implementing.

---

> Generated by `adlc-flow init` on YYYY-MM-DD. Customize the bracketed sections above. Read `.claude/CONTEXT.md` for vocabulary + gates + modes shared across all skills.
